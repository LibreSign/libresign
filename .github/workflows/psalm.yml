# This workflow is provided via the organization template repository
#
# https://github.com/nextcloud/.github
# https://docs.github.com/en/actions/learn-github-actions/sharing-workflows-with-your-organization
#
# SPDX-FileCopyrightText: 2022-2024 Nextcloud GmbH and Nextcloud contributors
# SPDX-License-Identifier: MIT

name: Static analysis

on: pull_request

concurrency:
  group: psalm-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  static-analysis:
    runs-on: ubuntu-latest

    name: static-psalm-analysis
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
          submodules: true

      - name: Get php version
        id: versions
        uses: icewind1991/nextcloud-version-matrix@8a7bac6300b2f0f3100088b297995a229558ddba # v1.3.2.3.1.3.2

      - name: Checkout Nextcloud server for Psalm context
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          repository: nextcloud/server
          ref: ${{ steps.versions.outputs.branches-max }}
          path: nextcloud-server
          persist-credentials: false
          fetch-depth: 1
          sparse-checkout: |
            3rdparty/doctrine/dbal/src
            3rdparty/sabre/dav/lib/DAV
            3rdparty/sabre/event/lib
            3rdparty/stecman/symfony-console-completion/src
            apps/dav/lib/Connector/Sabre
            apps/files/lib
            apps/files_sharing/lib
            apps/settings/lib/Mailer
            core/Command
            lib

      - name: Mirror Nextcloud server tree for Psalm
        run: |
          shared_root="$(dirname "$(dirname "$GITHUB_WORKSPACE")")"
          ln -sfn "$GITHUB_WORKSPACE/nextcloud-server/3rdparty" "$shared_root/3rdparty"
          ln -sfn "$GITHUB_WORKSPACE/nextcloud-server/apps" "$shared_root/apps"
          ln -sfn "$GITHUB_WORKSPACE/nextcloud-server/core" "$shared_root/core"
          ln -sfn "$GITHUB_WORKSPACE/nextcloud-server/lib" "$shared_root/lib"

      - name: Check enforcement of minimum PHP version ${{ steps.versions.outputs.php-min }} in psalm.xml
        run: grep 'phpVersion="${{ steps.versions.outputs.php-min }}' psalm.xml

      - name: Set up php${{ steps.versions.outputs.php-available }}
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: ${{ steps.versions.outputs.php-available }}
          extensions: bz2, ctype, curl, dom, fileinfo, gd, iconv, intl, json, libxml, mbstring, openssl, pcntl, posix, session, simplexml, xmlreader, xmlwriter, zip, zlib, sqlite, pdo_sqlite
          coverage: none
          ini-file: development
          # Required: re-enable pcntl_* functions that may be disabled via disable_functions
          ini-values: disable_functions=
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Install dependencies
        run: |
          composer remove nextcloud/ocp --dev --no-scripts
          composer i

      - name: Check for vulnerable PHP dependencies
        run: composer require --dev roave/security-advisories:dev-latest

      - name: Install nextcloud/ocp
        run: composer require --dev nextcloud/ocp:dev-${{ steps.versions.outputs.branches-max }} --ignore-platform-reqs --with-dependencies

      - name: Run coding standards check
        run: composer run psalm -- --threads=1 --monochrome --no-progress --output-format=github
